export enum AuthTag {
  USERNAME = 'username=',
  OLD_PASSWORD = '&oldPassword=',
  NEW_PASSWORD = '&newPassword='
}

export enum ElementTag {
  A = 'a'
}

export enum TokenAuth {
  KEY = 'TOKEN_AUTH_KEY'
}
