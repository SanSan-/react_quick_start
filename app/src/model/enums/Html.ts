export enum NotificationTag {
  E = '&eTag='
}

export enum AuthTag {
  USERNAME = 'username=',
  PASSWORD = '&pas' + 'sword=',
  OLD_PASSWORD = '&oldPassword=',
  NEW_PASSWORD = '&newPassword='
}

export enum ElementTag {
  A = 'a'
}
